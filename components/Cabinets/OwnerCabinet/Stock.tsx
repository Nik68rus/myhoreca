import React from 'react';
import { IShop } from '../../../models/shop';

interface Props {
  shop: IShop | undefined;
}

const Menu = ({ shop }: Props) => {
  return <div>Menu</div>;
};

export default Menu;
