import React from 'react';
import { ICompany } from '../../../models/company';

interface Props {
  company: ICompany | undefined;
}

const Menu = ({ company }: Props) => {
  return <div>Menu</div>;
};

export default Menu;
